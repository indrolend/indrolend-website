# Data Directory

> **Do not edit these files by hand.** All files here are auto-generated by automation.

---

## Runtime files (do not move)

These files are served at runtime by the legacy MPA. Their paths are hard-coded in
`legacy/js/` — do not rename or relocate them.

| File | Generated by | Read by |
|------|-------------|---------|
| `parsed-stats.json` | `scripts/ocr/parse_screenshots.py` | `legacy/js/spotify-analytics-data.js` |
| `spotify_stats.json` | `scripts/spotify/scrape_spotify_artists.py` | `legacy/js/spotify-artists-stats.js` |
| `spotify_stats_test.json` | Manual / test fixture | `legacy/pages/spotify-artists-test.html` |

---

## Future subdirectories (placeholder)

These subdirectories exist for future organized pipeline outputs:

| Directory | Intended use |
|-----------|-------------|
| `data/ocr/extracted/` | Raw OCR text output (future) |
| `data/ocr/parsed/` | Structured OCR parse results (future) |
| `data/spotify/parsed/` | Structured Spotify data (future) |

---

## Pipeline inputs

Pipeline inputs (screenshots, exports) live in `inputs/`, not here.
See `inputs/README.md` and `scripts/README.md` for the full pipeline flow.
